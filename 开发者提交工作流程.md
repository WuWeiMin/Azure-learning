# 开发者日常提交工作流程

配合《Git 分支与提交规范手册》使用，说明单个开发人员在开发一个新功能时，从建分支到合并，具体每一步该怎么操作、commit 该怎么处理。

---

## 核心原则

- **本地 feature 分支 = 草稿本**：想怎么提交怎么提交，不用有心理负担。
- **PR 前 rebase 整理 = 给别人看的整洁稿**：把碎片提交整理成几个语义清晰的 commit。
- **Squash Merge = 最后一道保险**：不管你整理得好不好，最终进入 `develop`/`release`/`main` 的永远是一条干净的记录。

---

## 第一步：从 develop 切出 feature 分支

```bash
git checkout develop
git pull
git checkout -b feature/cr1024-xxx
```

---

## 第二步：开发阶段——随便 commit，不用纠结

一个新功能可能涉及几十个文件，正常来说也不会一次性全改完。建议**按"完成一个可独立说明的小改动"就提交一次**，而不是攒一大坨再提交：

```bash
git add src/api/order.controller.ts
git commit -m "feat(api): 新增订单创建接口骨架"

# ...继续改...
git commit -m "wip: 加校验逻辑"
git commit -m "fix typo"
git commit -m "wip: 联调"
```

这些中间 commit 的 message 写得随意、提交得频繁都没关系——它们只存在于本地/feature 分支上，不会进入最终历史。

---

## 第三步：发 PR 前——用 rebase 整理成清晰的几个 commit

这是消除噪音的关键一步。用交互式 rebase 把 `wip`、`fix typo` 这类中间提交合并进它们所属的那次改动：

```bash
git rebase -i origin/develop
```

在打开的编辑器中，把要合并的行从 `pick` 改成 `squash`（或简写 `s`），保留第一条为 `pick`：

```
pick a1b2c3d feat(api): 新增订单创建接口骨架
squash b2c3d4e wip: 加校验逻辑
squash c3d4e5f fix typo
squash d4e5f6a wip: 联调
```

整理完，一个功能大概会剩下几个"讲得清楚"的 commit，而不是一个几十文件的巨型 commit，也不是几十条 wip：

```
feat(api): 新增订单创建接口
feat(ui): 新增订单创建表单页面
test(api): 补充订单创建接口单元测试
```

> 💡 **粒度建议**：前端+后端+测试拆成 2~4 个逻辑清晰的 commit 比较合理。太碎（几十个文件对应几十个 commit）和太粗（一个 commit 塞几十个文件）都不好 review、也不好单独 revert。

---

## 第四步：推送分支

rebase 会重写本地历史，如果之前已经 push 过，需要用 `--force-with-lease`（比 `--force` 更安全，会检查远程是否有他人新提交，避免误覆盖）：

```bash
git push origin feature/cr1024-xxx --force-with-lease
```

---

## 第五步：发起 PR

- PR 标题使用 Conventional Commits 格式，例如：`feat(order): 新增订单创建功能`。
- PR 描述中说明变更目的、关联的 CR/工作项编号、测试方式，如有破坏性变更需注明。
- 目标分支：`feature/* → release/*`。

---

## 第六步：合并——交给 Squash Merge 兜底

`feature/* → release/*`、`release/* → main` 的 PR 统一使用 **Squash Merge**。无论本地留了多少 commit，最终合入目标分支的都会被压缩成**一条**，commit message 取自 PR 标题。

这也是为什么第四步可以放心 `--force-with-lease` 重写自己的分支历史——反正最终合并时会被压成一条，不影响 `release`/`main` 上的干净记录。

---

## 完整流程速览

```bash
# 1. 建分支
git checkout develop && git pull
git checkout -b feature/cr1024-xxx

# 2. 开发阶段，自由提交
git add . && git commit -m "wip: ..."
# ...重复多次...

# 3. 发 PR 前，整理提交历史
git rebase -i origin/develop
# 编辑器中把中间提交改为 squash，合并成 2~4 个清晰 commit

# 4. 推送
git push origin feature/cr1024-xxx --force-with-lease

# 5. 发起 PR（标题用 Conventional Commits 格式）
#    feature/cr1024-xxx -> release/xxx

# 6. Review 通过后，Squash Merge 合入 release 分支
```

---

## 一句话原则

> 本地随便提交，PR 前整理一次，Squash Merge 兜底最后一道防线——三层保障下，`develop`/`release`/`main` 上的历史永远干净可读。
